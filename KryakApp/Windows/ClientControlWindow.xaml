<Window
    x:Class="KryakApp.Windows.ClientControlWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Client Control">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="WindowTitleBar"
                  Grid.Row="0"
                  IsBackButtonVisible="False"
                  IsPaneToggleButtonVisible="False"/>

        <Grid Grid.Row="1" Padding="22">
            <StackPanel Spacing="14" MaxWidth="900" HorizontalAlignment="Stretch">
                <TextBlock Text="Client Control"
                           FontSize="26"
                           FontWeight="SemiBold"
                           Margin="0,0,0,2"/>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <Grid ColumnSpacing="18" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Tag" Opacity="0.7"/>
                        <TextBlock x:Name="TagTextBlock" Grid.Column="1" FontWeight="SemiBold"/>

                        <TextBlock Grid.Row="1" Text="Username" Opacity="0.7"/>
                        <TextBlock x:Name="UserTextBlock" Grid.Row="1" Grid.Column="1" FontWeight="SemiBold"/>

                        <TextBlock Grid.Row="2" Text="IP" Opacity="0.7"/>
                        <TextBlock x:Name="IpTextBlock" Grid.Row="2" Grid.Column="1" FontWeight="SemiBold"/>

                        <TextBlock Grid.Row="3" Text="OS" Opacity="0.7"/>
                        <TextBlock x:Name="OsTextBlock" Grid.Row="3" Grid.Column="1" FontWeight="SemiBold"/>

                        <TextBlock Grid.Row="4" Text="Ping" Opacity="0.7"/>
                        <TextBlock x:Name="PingTextBlock" Grid.Row="4" Grid.Column="1" FontWeight="SemiBold"/>
                    </Grid>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="10">
                        <TextBlock Text="Client Actions" Opacity="0.8"/>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Button Content="Restart client"
                                    Click="RestartClientButton_Click"/>
                            <Button Content="Close client"
                                    Click="CloseClientButton_Click"/>
                            <Button Content="Delete client"
                                    Click="DeleteClientButton_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
