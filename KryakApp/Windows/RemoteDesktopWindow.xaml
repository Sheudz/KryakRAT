<Window
    x:Class="KryakApp.Windows.RemoteDesktopWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Remote Desktop">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="WindowTitleBar"
                  Grid.Row="0"
                  IsBackButtonVisible="False"
                  IsPaneToggleButtonVisible="False"/>

        <Grid Grid.Row="1" Padding="22" RowSpacing="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <ComboBox x:Name="MonitorCombo"
                          PlaceholderText="Monitor"
                          MinWidth="160"
                          SelectionChanged="MonitorCombo_SelectionChanged"/>
                <ComboBox x:Name="QualityCombo"
                          MinWidth="140"
                          SelectedIndex="2"
                          SelectionChanged="QualityCombo_SelectionChanged">
                    <ComboBoxItem Content="Very Low" Tag="10"/>
                    <ComboBoxItem Content="Low" Tag="25"/>
                    <ComboBoxItem Content="Medium" Tag="50"/>
                    <ComboBoxItem Content="High" Tag="80"/>
                    <ComboBoxItem Content="Native" Tag="100"/>
                </ComboBox>
                <Button x:Name="StartButton"
                        Content="Start"
                        MinWidth="90"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="StartButton_Click"/>
            </StackPanel>

            <Border Grid.Row="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12"
                    MinHeight="200">
                <Image x:Name="DesktopImage"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </Grid>
</Window>
