<Window
    x:Class="KryakApp.Windows.RemoteConsoleWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Remote Console">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="WindowTitleBar"
                  Grid.Row="0"
                  IsBackButtonVisible="False"
                  IsPaneToggleButtonVisible="False"/>

        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12"
                Margin="18,10,18,8">
            <TextBox x:Name="ConsoleOutputTextBox"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     PlaceholderText="Console output will appear here..."
                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        </Border>

        <Grid Grid.Row="2" Margin="18,0,18,18" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="CommandTextBox"
                     PlaceholderText="Type command"
                     KeyDown="CommandTextBox_KeyDown"/>

            <Button Grid.Column="1"
                    Content="Send"
                    MinWidth="88"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="SendButton_Click"/>
        </Grid>
    </Grid>
</Window>
