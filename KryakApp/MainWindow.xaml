<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="KryakApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryakApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="KryakApp">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!--  TitleBar  -->
            <RowDefinition Height="*" />
            <!--  NavigationView  -->
        </Grid.RowDefinitions>

        <TitleBar x:Name="titleBar" IsBackButtonVisible="False" IsPaneToggleButtonVisible="True" PaneToggleRequested="TitleBar_PaneToggleRequested"/>

        <NavigationView x:Name="navView" Grid.Row="1" IsBackButtonVisible="Collapsed" IsPaneToggleButtonVisible="False" SelectionChanged="navView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Server"
                                    Tag="Server"
                                    Icon="World"/>

                <NavigationViewItem Content="Nodes"
                                    Tag="Nodes"
                                    Icon="AllApps"/>

                <NavigationViewItem Content="Builder"
                                    Tag="Builder"
                                    Icon="Edit"/>

                <NavigationViewItem Content="Logs"
                                    Tag="Logs"
                                    Icon="Library"/>

            </NavigationView.MenuItems>
            <Frame x:Name="navFrame" />
        </NavigationView>
    </Grid>
</Window>
